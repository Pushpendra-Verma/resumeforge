/**
 * Template constants kept free of any React/component imports so that
 * non-UI modules (e.g. the document store) can reference the default
 * template id without pulling renderer code into their bundle.
 */
export const DEFAULT_TEMPLATE_ID = "iim-professional";
